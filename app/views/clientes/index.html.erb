<%= form_for :clientes , :url => {:controller => 'clientes' , :action => 'buscar'} do |f| %>
	Selecione um arquivo CSV: <%= f.file_field :nome_arquivo %> 
	<%= f.submit 'Submeter arquivo' %>
<% end %>